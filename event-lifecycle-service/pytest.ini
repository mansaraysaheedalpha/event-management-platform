# pytest.ini

[pytest]
# This tells pytest to add the current directory to Python's path
# so that imports like 'from app.main import app' work correctly.
pythonpath = .

# This tells pytest where to automatically find all your test files.
testpaths = tests