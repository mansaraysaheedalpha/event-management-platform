# requirements.txt

# FastAPI framework and server
python-dotenv
fastapi
uvicorn[standard]

# Database and ORM
sqlalchemy
alembic
psycopg2-binary
sqlalchemy-utils

# Data validation and settings management
pydantic
pydantic-settings
email-validator  # Required for Pydantic EmailStr validation

# Security and JWT
python-jose[cryptography]
passlib[bcrypt]
PyJWT[crypto]>=2.8.0
cryptography>=41.0  # RSA key generation for RS256 QR signing
slowapi  # Rate limiting for FastAPI

# For inter-service communication
redis
httpx
kafka-python-ng # <-- The modern, correct Kafka library
boto3
resend
jinja2  # For email template personalization
anthropic  # For AI-powered message generation
icalendar  # For ICS calendar file generation
africastalking  # SMS gateway for check-in PINs (Africa's Talking API)

# For background tasks
celery
pdf2image
apscheduler  # Scheduler for waitlist background tasks
tenacity  # Retry logic with exponential backoff for resilient operations

# Payment processing
stripe

# Analytics and reporting
pandas
openpyxl  # For Excel export
scipy  # For A/B test statistical significance

# GraphQL and Federation
strawberry-graphql[fastapi]  # Includes DataLoader for batching N+1 queries

# --- TESTING ---
pytest
